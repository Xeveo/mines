import { style } from "@vanilla-extract/css";

export const buttonClass = style({
  minWidth: "100%",
  minHeight: "100%",
  borderRadius: 0,
  lineHeight: "1.5em",
});

export const cellClass = style({
  height: "2em",
  width: "2em",
});
